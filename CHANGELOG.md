# 更新日志

格式参考 [Keep a Changelog](https://keepachangelog.com/zh-CN/1.1.0/)，版本号遵循语义化版本。

只记**对使用者有影响**的改动。纯重构、内部整理不列 —— 那些看 git log。

> 这份日志是 2026-08-16 补写的：v0.4.0 到 v1.13.1 之间有 81 个 tag，其中大量是同一件事拆成
> 的连续小版本（最多的一天发了 14 个）。补写时按**功能里程碑**归并，patch 版本折进所属条目，
> 不逐个罗列。此后新版本按正常粒度追加。

---

## [Unreleased]

### 新增

- **选项卡多了两条出路：自己写答案、跳过这题。** 以前只能在模型给的 2-4 个选项里挑一个，
  而它列的选项不可能覆盖所有做法 —— 想说第五种只能干等超时、或者另起一句话说。
  - **自己写一个答案**：终端菜单末尾多一项「✎ 自己写一个答案…」；全屏 TUI 里直接用底下
    那个输入框作答，不另弹窗口。工作台那张卡上是「✎ 其他…」。
  - **跳过这题**：明确把这一项交回给模型，按推荐项继续。它和"没人理"的区别是**留了痕** ——
    收尾说明里会写清楚这几项是你跳过的，不是没人在。
  - 顺手修掉一处会撒谎的地方：以前只看"答案是不是空的"来判超时，于是用户明明点了跳过，
    收尾却说"你在 5 分钟内没有选择"。现在按通道回的是不是 None 来分。
  - 详见 [ADR-0031](./docs/decisions/0031-a-running-turn-can-still-be-talked-to.md) 的补记。
- **知识型提问快了一大截。** "什么是 X"、"了解过 X 吗"、"X 和 Y 有什么区别"这类问题现在
  走新的 `quick` 车道：只挂 9 个只读检索工具（约 1032 token），而不是全量 59 个（约 8296
  token）—— 工具 schema 每一步都要重发，实测它占单轮上下文的 65%。知识库检索和引证照旧，
  只是不再带着一身用不上的写/执行/板块工具。
  - 实测（同一时刻交替跑、只换工具集）：墙钟 37.4s → 26.3s、78.1s → 24.4s，而且快得更稳；
    每 token 的生成速度也从 47~79ms 降到 29~32ms —— prompt 越短，模型吐字本身也越快。
  - 同时给这条车道一条直达提示：**检索最多一轮**，拿到资料就作答，不要为了确认再搜第二遍。
    裁工具省的是每一步的 token，省不掉步数 —— 实测裁完工具后模型仍然把同样的搜索跑了两轮，
    加上这句之后一轮里的工具步从 4 步降到 2 步。
  - 判不准一律回原来的路线：工程词、动作词、指向你自己数据的词（"我的"、"店铺"、"报表"、
    "最近"…）命中任意一个就退回常规路线。业务词故意不拦 —— "ACOS 是什么意思"要查的正是
    知识库，而 `knowledge_search` 就在这条车道的工具集里。
  - `chat` / 板块 / 常规三条路线的行为一个字都没改。
  - 详见 [ADR-0037](./docs/decisions/0037-quick-lane-for-knowledge-questions.md)。
- **回答可以自己配图了。** 新增只读工具 `web_images`：搜一批相关网页，把它们各自声明的
  分享预览图（`og:image` / `twitter:image`）取回来，返回可直接写进正文的 markdown。问"某某
  公司是做什么的"这类问题时，正文旁边就能有一张真实的图，而不是纯文字。
  - 零 key、零成本，不依赖任何图片搜索服务；实测一次配图 1.5~3 秒。
  - 只挑够格当配图的：按真实像素尺寸筛（≥320×200），图标、头像、装饰条一律排除。
  - 有 20 秒总预算，慢站到点就放弃，绝不拖住整轮回答；一张都配不到时明确说"配不出图"。
  - 系统提示词给了边界：具体的公司/产品/地点/实物/界面/人物才配图，抽象概念、纯数字结论、
    代码问题不配。要**新画**一张图仍然用 `image_generate`。
  - 结果里连来源页摘要一起给，模型一次调用就同时拿到图和资料，不必先 `web_search` 再
    `web_images`（实测那一轮为此白走了两步，每步都是一次完整的模型往返）。
  - 详见 [ADR-0036](./docs/decisions/0036-illustrations-come-from-og-image.md)。

---

## [v1.16.3] - 2026-08-30

### 新增

- **广告否词加了「不能否」护栏。** 生产广告引擎的否词杠杆过去是纯阈值（15 点击 0 单就建议
  否定），没有任何词性判断——品牌词天然就是"高点击、当期 0 单"的形态，正好被它否掉，而否掉
  品牌词是直接掉销量的。现在品牌词 / 战略大词 / 竞品词 / CTR 远高于账户均值的词（那是
  Listing 承接问题，不是流量问题）一律拦下并给出理由。
  - 新增可选设置 `lingxing_brand_tokens` / `lingxing_competitor_tokens` /
    `lingxing_strategic_tokens`，**默认全空 = 行为和以前一样**，只是候选上会多一条
    "未配置品牌词，无法排除品牌词误否"的提醒。单词和多词短语（"anker soundcore"）都认。
  - 判不了的不静默放行：统计上还不够（按账户 CVR 算这么多点击拿 0 单的概率）、经济上还不够
    （花费尚未超过单笔毛利）、疑似竞品词（推断）都会作为警告出现在报告里。
  - 「新品期差词」这条护栏当前数据面支撑不了，报告里明文列进"护栏未覆盖项"。
  - 取舍见 [ADR-0034](./docs/decisions/0034-negation-guardrails-live-in-ivyea-not-the-vendored-script.md)。
- **`ivyea answer-eval`：评测终于测"回答对不对"，而不只是"卡片召回对不对"。** 真跑主脑生成
  回答，再用分域 rubric 判分。慢、要花钱、分数有波动，所以是独立命令，不进测试套、不挂定时任务。

- **两张一直缺的知识卡：跟卖/购物车（Featured Offer）与差评/评论政策。** 问"有人跟卖我的
  链接怎么办""最近差评变多了怎么办"过去召不回任何官方依据。两张卡都带**诊断路径**（先分清
  是哪一类问题 → 逐步取证 → 动作 → 护栏），来源已逐条核实亚马逊官方页。
- **`ivyea knowledge gaps`：知识覆盖薄弱处清单。** 按证据强度从弱到强排，告诉你下一批该补
  哪些卡。**一开始是空的**——它要靠真实提问攒，用一段时间再回来看。
  - 为什么不自动判定"这是不是缺口"：两种判据都实测不可靠（问一个库里没有的机制，照样会有
    官方卡被语义近似捞上来）。如实记录、排序给人看，不冒充结论。
  - 缘由见 [ADR-0035](./docs/decisions/0035-knowledge-gaps-must-build-their-own-priority-list.md)。
- **知识索引可以用稠密向量了**（`ivyea retrieval --dense` + `retrieval sync`）。**默认仍是关的**：
  打开后第一次同步要把全部分块过一遍模型（本机约 2.5 分钟），那种一次性开销不该藏在随便一条
  命令里发生。
  - 挡在前面的两件事都解决了：同步改成**增量**（只重编真正变了的分块，二次同步实测 0.2 秒），
    搜索**绝不再同步重建索引**（那等于把重建塞进每条消息都走的热路径）。

### 修复

- **问一个亚马逊没有的机制时，容易被顺着编圆。** 证据里一张权威卡都没有、或者问的是具名
  机制（某某认证/等级/权重/指数）时，现在会把"证据标准"护栏卡一并给到模型。最危险的不是
  "没有证据"，而是只有一堆用户上传的长文章——手里有材料又没官方依据时最容易编。
- **编码提问会被误判成亚马逊问题。** "编译链接报错 undefined symbol""帮我写个用户注册页面"
  过去都会触发亚马逊知识检索，把一大段亚马逊证据注进编码对话。根因是"报错""错误码""注册"
  这些**通用词**既在高风险词表里、又在域词表里，等于自己给自己提供"这是亚马逊话题"的证据。
- **中文口语提问检索不到东西。** 问"广告花了钱不出单"过去是**零命中**，"链接突然没曝光了"
  甚至不会触发检索。根因是中文没有分词（整句被当成一个词）、打分没有文档长度归一化（用户
  上传的长文章把官方卡挤出候选池）、域门控只认术语不认运营口语。
  - 摘录不再是卡片开头那段 `Source type / Source URL / Retrieved at` 元数据——那些字段在引证
    行里已经给过模型一遍，白占注入预算。现在取命中最密集的正文片段。
  - 证据条数多了反而看到更少的问题也修了：过去是拼完超长砍尾巴，会把后面的引证整条切掉；
    现在按证据优先级分配预算。
  - 站点加分过去只认日本站和英国/欧洲站，加拿大 / 墨西哥 / 新加坡 / 印度 / 澳洲 / 中东站
    只能靠碰运气；现在由卡片自己的站点字段驱动。
  - 注入延迟从 350–410ms 降到 31–96ms。
  - 取舍见 [ADR-0033](./docs/decisions/0033-retrieval-fusion-and-answer-level-evals.md)。
- **调 bid 的步长和冷却期，说的和做的不是一套数。** 提示词里写"单次降 bid ≤20%、5 天稳定期"，
  playbook 写"10-20%"，而引擎实际执行的是 15% 步长、7 天冷却。统一按引擎的口径，并标明这是
  本系统的运营设置、不是亚马逊官方规则。

## [v1.16.2] - 2026-08-30

### 新增

- **会话附件：传给它看一眼的文件，不再被塞进知识库。** 新增 `POST /v1/files/extract`，
  只把文档抽成正文，**不写知识库、不建索引、不留档**。抽取用的是和知识库入库同一个
  函数，所以两条路读出来的字一模一样。
  - 附件按 `kind` 分流：`document` 走独立的 `[用户附件 —— 文档正文]` 注入段，有自己的
    份数（4 份）和字数上限（60000）——不再和附图挤同一个池子（那个池子是按"一张图的
    文字描述"定的 6000 字，一份 PDF 进去就被腰斩，而且贴 4 张图就能把文档挤没）。
  - 注入段里明确告诉模型这些文档**没有进知识库**，免得它回一句"我在知识库里找到"，
    或者反过来因为知识库搜不到就说没有这份材料。
  - 缘由与取舍见 [ADR-0032](./docs/decisions/0032-session-attachments-are-not-knowledge.md)。
- **`show_image`：把一张图给用户看。** 你截的图、跑出来的图表、读到的产品图，现在能
  真的展示出来，而不是只用文字描述。一个工具名两种形态：嵌在 IvyeaOps 里时委托给
  宿主，网页上直接渲染成图（权限边界也在宿主那边，这边不另开一套）；在终端里不渲染
  画面，只确认这确实是一张图并报出绝对路径。**绝不往 stdout 打字** —— `-p
  --output-format stream-json` 下 stdout 是协议通道。
- **会话记下自己是从哪儿开的。** 落盘新增 `origin`（`cli` = 终端里敲的 `ivyea chat`）
  和 `cwd`（开会话时所在目录），`/v1/chat/sessions` 一并透出。IvyeaOps 任务台左栏靠它
  把终端会话标成「终端」—— 在这之前它们混在列表里，没有来源也没有归属，看着像一堆
  无主会话。老会话没有这两个字段，取空串，不受影响。

### 修复

- **二进制文件不再被当成"正文"注进上下文。** `.zip` 这类文件此前会"成功抽出"几万个
  控制字符、一个警告都不给（抽取只在结果短于 20 字时才报警），然后被整段塞给模型 ——
  既烧上下文又帮不上忙。现在按可打印字符占比判定，命中就把正文清空并报
  `looks_binary`，由调用方给用户一句人话。

---

## [v1.16.1] - 2026-08-28

### 新增

- **`-p` 也有输入通道了**：`ivyea chat -p --input-format stream-json` 会从 stdin 逐行读
  控制消息。此前 `-p` 是一条单行道（喂一句、读一串、退出），中间那几十分钟里调用方
  没有任何一条路走回来 —— 于是在 IvyeaOps 的 /agents 聊天里，ivyea 这一档既插不进话、
  也弹不出选项卡，想停只能 SIGTERM，而那样**这一轮跑出来的东西一个字都不落盘**。
  - `{"type":"user_input","text":"…"}` —— 追加指令，在下一个步边界插进当前这一轮，
    插进去回一条 `injected` 事件供调用方销账。
  - `{"type":"control_response","request_id":"…","response":{"answers":{…}}}` ——
    回答 `ask_user_question` 弹出的选项卡（进程侧发 `control_request`）。
  - `{"type":"interrupt"}` —— **优雅中止**：在下一个安全点收摊，已经跑出来的正文/
    执行过程/时间账照常落盘，回一条 `result.subtype=cancelled`，退出码 0。
  - 不带这个开关时行为逐字不变（stdin 照常不读）。

## [v1.16.0] - 2026-08-28

### 新增

- **一轮跑着的时候可以补话了**。此前轮次一开跑，输入通道就关了：想加一句"顺便把
  X 也改了"只能干等几十分钟，或者掐掉重说（前面的上下文一起作废）。现在追加的
  指令会在两个工具步之间**插进当前这一轮**，模型下一步就看得见；模型正要收工时
  也会再看一眼，所以"快跑完了才想起来补一句"同样算数。
  - 新端点 `POST /v1/chat/inject`（没有活轮时明确回 `accepted:false`，由调用方
    当成下一轮发出去，不会烂在服务端）。
  - 追加指令是用户真说过的话：历史里留着、自成一轮，不会被当成注回消息摘掉。
  - 没来得及读到的，收尾时随 `final.injected_pending` 端回给调用方。
- **拿不准的时候会把选项弹给你选**（新工具 `ask_user_question`）。方案分叉时给
  2-4 个选项、每个写清楚选它会发生什么，并标出推荐项。
  - **五分钟没人选就按推荐项继续**，任务不会卡在一个没人看的问题上；超时时长可用
    `ivyea config set ask_timeout_seconds N` 调。
  - 自动定的项会记账并随 `final.auto_decisions` 端出去，界面据此说明"这几项是替
    你定的"；工具也会要求模型在收尾总结里复述一遍。
  - 无人值守（cron / 飞书 / 管道）下不等待，直接按推荐项走。
  - 每轮最多问 3 次；子 agent 不给这个工具。
  - 一次问多问、人只点了其中几问时，**剩下那几问同样按"替他定的"记账**
    （真跑那一轮里模型问了四问、只答了一问，另外三问差点无声地被自动填掉）。
- **每一轮的起止时刻和时长会落盘**（`turn_times`，与消息平行存）。工作台据此显示
  "发送于 09:46 / 结束于 09:49 · 用时 3 分"，刷新、换标签页、换台机器打开同一条
  会话都还在。
- **`POST /v1/chat/cancel`：真的停掉正在跑的那一轮**。此前调用方能做的只是断开自己
  那条事件流 —— 轮次在这边照跑、照烧 token。现在中止标志由轮次线程在**模型流的每个
  事件和每个工具步边界**读取，所以点下去几百毫秒内就不再往下走。
  - 已经跑出来的正文、执行过程、时间账**照常落盘**（半截也是真发生过的）。
  - 收尾发 `cancelled` 事件而不是 `error` —— 用户改主意不是失败。
  - 正在执行中的那**一个**工具调用不会被打断（写文件、跑命令中途砸断只会留下半个
    现场），所以最坏要等它结束，但模型不会再往下走一步。
  - 停在半路时还没被读到的追加指令随 `cancelled.injected_pending` 端回调用方。
- **`GET /v1/chat/live-sessions`**：此刻真的有一轮在跑的会话 id（读内存，不扫会话
  文件），供界面给正在执行的会话打标记。

## [v1.15.19] - 2026-08-28

### 变更

- **计划不会再"走着走着就忘了"**。多步任务的计划此前只活在内存里，而且从来没有被
  喂回给模型 —— 它知道自己的计划，全靠那几条 `todo_write` 调用还留在对话历史里。
  于是长任务一压缩，计划就跟着蒸发，模型接着干活时已经不记得原本打算干几件事、
  干到第几件了。现在计划落盘（`~/.ivyea/plans/`），每轮由运行时注回上下文，
  **压缩时原样保留**，进程重启、会话续跑都还在。
  - `/plan show` 看当前计划（目标、完成标准、每步状态与证据、改过几次）。
  - `/approve`（以及手动退出计划模式、Shift+Tab 切出计划模式）会给计划盖批准戳。
  - 计划模式里产出、又没被批准的计划，回到执行档时不允许直接写操作 ——
    只有真的走过计划模式的会话才有这道闸，普通对话行为不变。
- **任务的步骤表和模型的计划，从此是同一份**。此前 `ivyea task` 那张步骤表只有模型
  显式调 `task_step` 才会动，而模型实际维护的是 `todo_write` —— 一份计划两处记，结果
  就是**续跑提示照着一份过期的步骤表指路**（"下一步 #2"，而模型早就干到第 4 步了）。
  现在计划台账是唯一真相，任务里的步骤是它的投影：
  - 模型每更新一次计划，`ivyea task show` / `/v1/task/{id}` / 续跑提示看到的就是最新状态，
    每步还带上它真正拿到的证据。
  - 反过来，**你在任务里排好的步骤，agent 接手时看得见了**：会话绑了任务而计划还空着时，
    任务步骤会成为计划的起点，注回模型上下文。此前它只能从续跑提示里读到一句散文。
  - 换一轮对话会清空计划，但**不会**抹掉你排的任务步骤；而且已推进的进度能顺着任务文件
    活下来。人工取消（`cancelled`）的任务不会被同步改回"进行中"。

- **原地打转会被拦下来**。同一个工具用**完全相同的参数**连调 3 次，或者连续 8 步
  没有产生任何新证据（没写成东西、没跑通命令、没读到新内容、结果一字不差），
  会被拦住并要求换思路或重新规划，而不是一路烧到步数上限才收场。
  - **同一个工具连续拿回一句一模一样的拒绝**同样算打转。实测里模型连发 90 次
    `progress_update`，每次只把措辞改一点，就绕过了"按参数看重复"那条检测 ——
    现在按结果看。中间成功一次、或换了一句拒绝，都会清零。
  - 轮询类（`bash_output`）豁免 —— 它本来就会一直拿回"无新输出"。
  - 阈值可调：`loop_guard_repeat_limit`（默认 3）、`loop_guard_stall_limit`（默认 8），
    设 0 即关掉对应检测。
- **被拒绝时会告诉你下一步该干什么**。汇报状态机此前有几句拒绝只说"不对"不说
  "该怎么办"（`phase_end 必须结束当前正在汇报的阶段`），模型只能反复换措辞重发，
  一轮就这么烧光了。现在会指名：先把第几步标 in_progress、还是 phase_index 指错了、
  还是所有步骤都已收尾该直接做最终汇总。同一个工具连续被拒 6 次（哪怕每次拒绝理由都不同）
  也会被拦下来，要求换条路或者把问题告诉你 —— 换着法子撞同一堵墙同样是打转。
- **"写结论"这种不调工具的收尾步骤，终于能标完成了**。此前 phase_end 要求"完成/部分完成的
  阶段必须有**本阶段的**真实工具结果"，于是「最后一步：写结论，不要调工具」这种再正常不过的
  收尾步骤永远收不了尾，模型会在这一步上反复碰壁直到把整轮耗光。"不能只凭文字声称完成"这条
  照样守，但改到**本轮**层面：本轮真跑出过工具结果，综合阶段就可以收尾（证据自动挂上本轮的
  真实结果）；一次都没跑通，才是真的空口，照旧拒绝并提示改成 blocked/skipped。
- **改过代码、下过写指令的那一轮，交付前会自己复核一遍**。此前 `self_critique` 全靠模型
  自觉去调 —— "该自查的时候自查"交给自觉等于没有。现在这两类轮次由运行时兜底跑一次，
  按域用不同的复核维度（代码问消费方契约和验证、广告问数据量和护栏、知识问官方事实与推断
  的分界）。实测在一次"加个除零保护"的小改动上，它指出了"调用方可能依赖 ZeroDivisionError"
  和"-0.0 会不会漏掉"两条真问题。
  - 只盯**真代码改动**和**真下过写指令**的轮次；普通问答、只写文档的轮次一次也不跑
    （自查要多花一次模型调用，不该给每次问答都加税）。
  - 复核判"通过"时静默放行，但**发现的问题仍会作为「自查备注」讲给你听** —— 付了钱就要
    拿到东西。拿不准一律按通过：误判成"要修"会逼模型把已经对的答案改坏。
  - 最多逼修正一轮。关掉：`critique_before_done=false`。
- **`ivyea` 现在记得住"到底验证过什么"**。命令、测试、读写、接口调用会落进证据台账
  （`~/.ivyea/evidence.db`，30 天过期），跨轮跨会话可查；最终汇报的「验证」一栏在模型
  自述不足时用它兜底 —— 此前那些痕迹只活在当前这一轮的内存里，换一轮就没了。
- **新增 `/think adaptive`：按这一轮的性质自动定思考深度**。寒暄降到 low（这条路线本来
  就连工具都不挂），其余一律维持 high；多步执行任务无论哪条路线都强制 high。
  - **默认仍是 `high`，不设置就完全不变。** 要用得显式开：`/think adaptive`。
  - 它和 `auto` 不是一回事：`auto` 是"交给模型自己决定"（Gemini 上是动态思考预算），
    含义没有任何改变。
- **技能终于能读全了**。此前自动注入把技能正文**截到 700 字**，而剩下的部分模型
  **根本够不着** —— 没有任何工具能读技能全文。于是一本三千字的审计手册，模型永远只看得到
  开头那段"何时使用"，真正的步骤和护栏全在截断线以下，它照着残缺的手册干活还以为自己看全了。
  现在注入改成「开头一段 + 一句"要全文用 `skill_view`"」，附属文件（`references/`、
  `scripts/`）也点名列出、按需读。
- **agent 可以自己沉淀技能了**。走完一套值得复用的流程后，它能用 `skill_write` 写成技能，
  写操作照旧走审批、计划模式一律拒绝。
  - `/learn`（对话里）和 `ivyea learn`（命令行）：把目录、文件、网页、"刚才做的那件事"
    学成技能。大素材（手册、规范、文档集）走"精简索引 + `references/` 分章"的布局，
    一章一写，不会把整份灌进上下文。
  - **落盘前会校验，不合格直接拒绝**：`triggers` 必填（检索不分词，中文全靠触发词命中 ——
    没有它中文用户根本搜不到）、name 要小写连字符、description 必填且不能是一整段。
    正文里提到不存在的工具名会被点出来。
  - `ivyea skill usage` 看哪些技能真在用；`ivyea skill archive` / `restore` 管生命周期，
    **只归档不删除**。内置技能不许归档。
- **技能检索接上了语义层**。此前是纯词法 2-gram + 一张手工停用词表，口语化的问法和技能里的
  正式用词对不上就是零命中。现在按记忆那套双路召回（词法 + 向量 RRF 融合）来找。
  - **自动注入那条路不受影响**：几十条技能是个小语料，向量永远会返回"最像的那几条"、
    没有"都不像"这个答案。想按语义找技能是 `skill_search` 的事，那时候你是在主动翻库。
  - 没有向量后端时，行为与纯词法**逐条相同**。关掉：`skill_semantic_search=false`。
- **修：`compact_at_tokens` 调得太低时，每一步都在压缩，每一步都白压**。自动压缩此前
  只问"用量越过阈值没有"，没问"压了有没有用"。压缩动不了 system 提示词，所以阈值一旦
  低于 system 本身（换小上下文模型时顺手调小、或手滑填了个小数字），用量就永远在阈值
  之上：实测 20 个工具步里发生 10 次摘要调用，钱照花，还把还能直接用的近期原文反复
  换成散文摘要。现在自动压缩会先算这次能压掉多少，压不动就不跑，并**明说一次**原因和
  该把 `compact_at_tokens` 调到多少之上 —— 此前它在日志里长得和正常压缩一模一样，
  除了账单没有任何地方看得出来。手动 `/compact` 不受影响，敲了就压。

- **修：读源码时，`$TOKEN` 这类变量引用会被当成密钥抹掉**。脱敏是加在所有工具输出上的，
  于是 `curl -H "X-Token: $REPORT_TOKEN"` 在模型眼里变成了 `X-Token=***REDACTED***` ——
  它据此得出"这个脚本硬编码了 token"的错误结论。危害不止于看错：**模型读到的每一份源码
  都可能被悄悄改写**，它照抄读到的那一行去 `edit_file`，`old` 永远匹配不上文件真实内容。
  现在变量引用、`os.environ[...]`、`<your-key>`、`xxxx` 这类占位符一律放行，真实密钥照抹。
- **修：`Authorization: Bearer <JWT>` 只抹掉了 "Bearer"，JWT 本身原样留在日志里**。
  现在认证方案前缀会连同后面的凭据一起抹掉。
- **子 agent 有分工了**。此前只有一个写死的「只读调研员」——查代码和审 Listing 用的是同一句
  「把交给你的问题查清楚」。现在有 6 个角色：`code_explorer`（定位实现/调用方/影响面）、
  `data_analyst`（拉数读数）、`listing_auditor`、`ads_reviewer`（复核广告动作：数据够不够、
  护栏有没有越）、`knowledge_auditor`（核对事实来源）、`researcher`（默认，行为与以前完全一样）。
  - 自己加角色：在 `~/.ivyea/agents/*.md` 写一份（frontmatter + 正文即 system prompt），
    同名会覆盖内置。`ivyea agents` 看当前有哪些。
  - **所有角色仍然是只读的**：子 agent 跑在后台，写操作的审批没有人能应答，
    所以不给它执行权。要算数就把数字带回主线算。
- **记账调用不再吃步数配额**。实测里最近 300 次工具调用有 112 次是 `progress_update`/
  `todo_write` 这类纯记账（一句「测试」跑了 18 步、17 步花在这上面）。它们不推进任务，
  却和真正干活的调用一样占配额——于是「步数上限」量的不是任务有多大，是模型有多爱记账。
  现在它们照常执行、照常显示，但不计进预算。
- **可以给一轮设成本上限了**（`chat_max_cost_cny`，**默认不设**）。步数上限是防跑飞、
  不是防烧钱：同样 200 步，读几个小文件和反复喂 20 万 token 差着两个数量级。
  设了之后越线会停下来汇报，并且**说清是撞了哪道闸**——步数超了是「说一句继续」，
  钱超了是「这轮已经花了多少，你要不要继续花」。
- **`/resume`：接着上一轮没做完的继续**。撞上限停下来时，「停在哪、已经证明了什么」
  会落进计划台账；`/resume` 把这些带进下一轮，不用你重述一遍。
- **`ivyea skill curate`：技能库该清理什么，它告诉你**。三类建议：长期没被命中的、
  触发词高度重合的（会互相抢命中）、`audit` 不合格的。
  - **默认只打印建议**，`--apply` 才动手，而且动手也只是归档（可 restore）。
  - 只碰你自建的技能；内置技能不动。重合与不合格**永不自动处理**——合并要判断哪条更好、
    补齐要写内容，这两件事得你拍板。
- **会话结束后自动沉淀技能（`skill_auto_learn`，默认关）**。开了之后，真干过活的会话
  结束时会在后台问一句「这套流程下次还会这么干吗」，值得才写成技能；一次性的具体任务不写。
  同一类流程**跨会话见过两次**才真的建，第一次先进待定区。
  默认关是因为技能会被自动注入进后续对话、实打实改变模型行为——先用一段时间 `/learn`
  看清它的水准再说。
- **修：撞"步数天花板"停下来时，报的话是错的**。那种情况其实是绝大多数步数花在了
  记账调用上（预算根本没动），却会显示"已达安全上限 N 步"，还建议你把 `chat_max_tool_steps`
  调大——而调它一点用都没有，瓶颈不在配额。现在会如实说清"干活只用了 N 次配额、
  M 次花在记账上"，并且明说调那个参数没用。
- **修：连着只在记账、一件实事不做，此前谁也拦不住**。`todo_write` 每次参数都不同、
  结果又都是成功，于是重复检测、拒绝连击、空转检测三道全都绕过去了。实测一轮跑满了
  15 个模型步全在写待办。现在连续 4 次全是记账就会被拦下来，要求先去执行或者直接说卡在哪。
- **修：自查和上下文压缩这两次模型调用没算进本轮成本**。它们是运行时自己发起的、
  用户看不见，漏在成本闸外面等于"本轮花了多少钱"报的是假数。
- **只写文档的一轮不再被逼着"跑一遍真实运行路径"**。此前只要问句里带"界面/显示/输出/
  颜色"这类词，哪怕这一轮只写了一份 `.md` 报告，收尾时也会被行为门禁挡下来要求跑运行
  验证 —— 文档没有运行路径可验。现在行为门禁只认真正的代码/配置文件改动。

---

## [v1.15.18] - 2026-08-27

### 变更

- **记忆开始在 IvyeaOps / 飞书 / 任务台里生效了**。此前记忆只在命令行 `ivyea chat`
  里有：serve 这条路（工作台对话、飞书、任务台走的都是它）从不注入核心记忆和记忆索引，
  模型不知道记忆库里有什么，也就不会去检索 —— 在网页端用等于没有记忆。同时这些对话
  也不进情景记忆，反思无米下锅，**越在网页端用、记忆越不长**。现在两头都补上了。
  - 不想让某一轮用记忆：`ivyea chat --no-memory`，或调用时传 `no_memory: true`
    （只关"写"，已有记忆照常读）。
  - 定时巡检、任务台续跑这类自动化轮次**默认不注入、也不记** —— 它们复用已有的
    `inject_retrieval` 开关，机器的例行输出不会被当成"用户的经历"。
- **反思（把零散经历提炼成长期记忆）改成后台跑**，不再卡在退出那一下。
  连带把触发门槛从 12 条经历降到 8 条 —— 门槛当初设那么高，一半是为了少让用户等。
  代价是巩固更勤、模型调用变多；可用 `memory_reflect_min_episodes` 调，
  `memory_auto_reflect=false` 整个关掉。想省钱可以给它单独配一个便宜模型：
  `memory_reflect_model`。
- **每轮开口前自动回忆**。此前得靠模型自己想起来去调 `memory_search` ——
  "忘了查"就是记性差最常见的原因。现在改成运行时每轮先查一遍，模型没有忘的机会。
  召回了什么会明确告诉你（工作台画一枚 🧠 芯片，命令行打一行），
  不用猜"记忆到底有没有生效"。
  - 寒暄和应答（"好的""收到""继续"）不触发检索 —— 那类话查不出东西，
    还会把上一个话题的残留带进来。这一条顺带也管住了知识库检索，
    此前说一句"好的"照样会跑一次。
  - 同一条记忆在一次会话里只注入一次，不会越聊堆得越多。
  - 自动召回只收**词法确有重合**的记忆；纯语义联想留给 `memory_search` 工具。
    自动注入宁可少给，也不能每轮塞几条不相干的记忆把回答带偏。
  - 关掉：`memory_auto_recall=false`；给的条数：`memory_auto_recall_limit`（默认 4）。
- 记忆作用域（按项目/店铺隔离）已打通，但**默认关**：开了之后跨项目的记忆互相看不见，
  可能表现为"以前想得起来的现在想不起来"。要试用 `memory_scope_from_workspace=true`。

- **记忆写坏了能回得去**：`USER.md` / `AGENTS.md` 是摆在 `~/.ivyea` 下、鼓励你亲手改的
  文件。现在如果 agent 看过之后、写入之前你改动了它，这次写入会被**拒绝**并备份当前内容，
  由 agent 重新读一遍再改 —— 而不是把你刚写的东西覆盖掉。
- **压缩上下文时会顺手沉淀记忆**：被压掉的那一段往往正是"结论是怎么来的"，
  此前只进这一条会话的摘要、换个会话就没了。关掉：`memory_reflect_on_compact=false`。
- **反思不再改得动你亲口定的规矩**：从行为反推出来的"你大概是这么要求的"，
  只会挂进待定区等你确认（`ivyea memory pending`），不会直接覆盖你说过的话。
- 记忆写入在同一轮里连续失败 3 次就停手，先把回答给你 —— 记不上是小事，
  为了记一笔把这一轮耗光是大事。

- **记忆终于看得见、管得着**。新增一组 `/v1/memory/*` 端点，IvyeaOps 的
  「知识库工作台 → 记忆」里可以：确认或驳回 agent 的推断、直接编辑那两份每轮常驻的
  核心记忆、搜索/查看/删除分类记忆、看三层记忆各自的健康状况、手动触发一次整理。
  界面上的写入和 agent 自己的写入走**同一条路**（同一套查重、冲突消解、历史归档），
  并且会被标成"你亲口说的"——从此 agent 的反思不许再改它。
  读要登录、写要管理员。

### 修复

- **对话记录不再只进不出**。serve 接进来之后每轮会往检索库加两行，此前从不清理，
  用久了会让每轮都要跑的记忆召回越来越慢。现在默认保留 180 天的对话行
  （`memory_episode_retention_days`，设 0 关闭）；会话摘要、决策、巡检记录一律保留。
  **首次清理前会自动备份 `memory.db`** —— 这是记忆里唯一不可逆的一步。
- serve 启动时不再漏掉 markdown 记忆的索引对齐 —— 手改过 `MEMORY.md`、
  或重装后 `memory.db` 丢失时，工作台这边不会再出现"文件里明明有、却检索不到"。

---

## [v1.15.17] - 2026-08-27

### 新增

- **正在跑的那一轮，别的地方也能看见**：新增 `GET /v1/chat/sessions/{id}/live`
  —— 任何客户端随时接进来，先回放这一轮已经发生的过程，再实时跟着往下跑。会话详情
  多回一行 `live`，说明这条会话现在有没有活轮。
  工作台据此修好了"切走再切回来 / 刷新 / 换台机器，执行过程整块消失"的老毛病
  （见 [ADR-0024](./docs/decisions/0024-a-running-turn-is-a-server-side-fact.md)）。
- 记忆那一组工具（`memory_write` / `memory_search` / `memory_read` /
  `core_memory_*`）终于有了中文名。此前界面上显示的是英文原名，还被那一列的定宽
  截成 `memory...`。

### 修复

- **模型在收尾阶段报错时，已经流出去的正文不再丢失**。此前落盘只写在"正常收尾"
  那一条路径上：额度用尽 / 断流时直接走人，用户眼前明明有一整篇回答，刷新之后
  一片空白，会话文件里也确实没有。现在正常收尾、模型报错、任意异常，一律先把
  已经产生的消息和步骤落盘。

- **Windows 上服务起不来**（升级到 v1.15.x 后出现）：`serve` 崩在开场白的一个 `✓` 上 ——
  `UnicodeEncodeError: 'gbk' codec can't encode character '\u2713'`。IvyeaOps 起 serve 时
  把输出重定向到日志文件 / NUL，此时 Windows 的 Python 不再用 UTF-8 而是退回系统代码页
  （中文机器 = GBK），编不出这个字符就整个进程退出，工作台那边只看得到
  "All connection attempts failed"。现在每个入口都会先把 stdout/stderr 钉成 UTF-8
  （见 [ADR-0023](./docs/decisions/0023-stdio-is-utf8-on-every-entry-point.md)）。
  英文版 Windows 同理受益 —— cp437 连中文日志都编不出来。

## [v1.15.16] - 2026-08-24

### 新增

- **巡检新增促销活动 4 条规则**（L1 快照层，随每小时巡检一起跑，命中即推飞书卡片）：
  - **活动即将结束** —— 结束前 24 小时、6 小时各提醒一次（6 小时那次是 crit）。
    提醒里带上这条活动**涉及哪些 ASIN**，不用再回后台自己查。
  - **活动即将开始** —— 开始前 24 小时提醒确认库存与价格。
  - **优惠券预算见底** —— 用掉 80% 提醒、95% 升为紧急。券的预算花完就不再展示，
    这跟活动到期一样要命，但后台不会告诉你。
  - **促销数据停更** —— 领星的促销数据是「LINGXING助手」浏览器插件抓的，插件掉线时
    接口照样返回 200 和旧数据，倒计时会安安静静地停在过期的值上。超过 24 小时没更新
    就报，因为此时**上面三条规则的结论全都不可信**。
  - 这四条都**不带一键执行按钮**：延长活动、加券预算、改折扣，在领星 OpenAPI 和
    亚马逊官方 API 上都没有写接口（促销本来就只能在卖家后台改）。给一个点了没用的
    按钮比不给更糟。
- **新增 `promotion.active` 指标**（优惠券 / 秒杀 / 管理促销 / 会员折扣），领星数据源
  已实现。活动列表接口不返回 ASIN，ASIN 是从商品维度接口按 promotion_id 挂回来的。
- **新增站点时区表**（`stores.timezone_name` / `stores.tzinfo`）。领星把促销时间给成
  站点当地时间的裸字符串，按服务器时区算，UK 的活动会差 7~8 小时。

### 修复

- **带货币符号的金额解析不出来**。促销接口的预算字段长这样：`"JP¥10,084.0"`。
  通用的数值转换只剥逗号和百分号，遇到货币前缀直接落回默认值 —— 于是预算永远是空，
  "预算见底"这条规则永远不会触发。

## [v1.15.15] - 2026-08-23

### 修复

- **serve 重启后会把自己上一轮的残留状态当成"外部服务在跑"，然后让位给一个
  根本不存在的服务**。判定"要不要让位"时读的是合并后的状态，而合并状态里包含
  进程内工人自己——自指。表现极具欺骗性：日志明明白白写着"系统 timer 已在跑，
  进程内不重复"，而实际上谁都没在跑，巡检就此静默停摆。
  外部探测改成**只问系统服务**；落盘状态另加一道"写它的进程还活着吗"。

## [v1.15.14] - 2026-08-23

### 修复

- **内建的飞书长连接跑着跑着会被自己判成"没在跑"**。长连接线程一旦连上就一直
  阻塞在 SDK 里，不再上报心跳；心跳过期后，别的进程（`ivyea relay status`、
  配置向导、doctor）就会一致地说它没运行，然后催用户去装一个**其实正在跑**的服务。
  加了一条独立的心跳循环，按"线程还活着吗"续，间隔取过期窗口的六分之一。

## [v1.15.13] - 2026-08-23

### 变更

- **巡检节拍器和飞书长连接改成跟着 `ivyea serve` 一起跑，不用再装两个系统服务、
  也不用点按钮**。前两版把它们塞进了包、又做成一键安装，但"还要点两下"依然是
  多余的一步 —— 装了 IvyeaAgent 就该直接能用。

  重看 ADR-1 当初把入站拆成独立服务的两条理由，今天只剩半条站得住：
  "强绑 lark_oapi 会让非中国用户装无用依赖"约束的是**依赖**（已做成可选 extra），
  不是**进程**；"长连接常驻会污染 CLI 进程模型"说的是 `ivyea chat` 那种短命进程，
  而 `serve` 本来就是常驻守护进程、早就有后台线程。所以入站可以跟着 serve 走。

  **跑两份会真出事，所以有两道闸**：外部已有 relay 服务或 systemd timer 在跑时，
  进程内的工人自动让位（两条长连接 = 同一次按钮点击可能执行两遍，因为去重表是
  进程内的；两个节拍器 = 同一份早报推两遍）；多开 serve 时用带过期的文件锁选一个。
  想要进程隔离的，`ivyea relay install` / `ivyea schedule install` 照旧可用。

  工人状态**跨进程可见**（落盘 + 心跳过期判定）：`ivyea relay status`、doctor、
  IvyeaOps 自检都是别的进程在问，只存内存的话它们会一致地报"没在跑"，
  然后催用户去装一个其实不需要的服务。

  可以关：`ivyea config set serve_worker_relay off` / `serve_worker_scheduler off`。

## [v1.15.12] - 2026-08-23

### 修复

- **巡检触发器也随包发布了，而且两样东西都能从界面一键装上**。
  上一版把飞书接收端搬进了包里，但装它还得开终端敲两条命令 —— 对只用网页的人，
  "请自行 pip install"等于"这个功能你用不了"。更隐蔽的是**巡检触发器**：
  它此前只以 `deploy/systemd/*.timer` 的形式躺在仓库里，wheel 里根本没有。
  后果是用户在界面上把巡检开关全打开、界面显示"已启用"，然后**永远不触发**，
  也没有任何报错 —— 缺的东西不在他机器上，报错也就无从产生。

  现在单元内容写在代码里跟着 wheel 走，配置页在检测到缺件时直接给出红色提示条
  和按钮：「一键安装接收端」（缺 SDK 会先装）、「一键启用触发器」。
  命令行同样可用：`ivyea relay install`、新增 `ivyea schedule install`。
  没有 systemd 的机器（Windows / macOS）不代劳，而是把该跑的命令原样写给你 ——
  launchctl / schtasks 的失败模式很难在看不见那台机器的情况下判断对错。

## [v1.15.11] - 2026-08-23

### 修复

- **飞书接收端随包发布了**。此前它是一个独立目录，**从来没有随任何版本发出去过** ——
  于是拿到这个包的人：巡检卡片收得到、早报收得到，但**点「批准执行」什么都不发生**，
  飞书里也没法和 agent 对话。方案里的 R2（飞书对话）和 R6（同意后直接执行）
  对外部用户等于不存在，而界面上那个按钮还看得见、点得动 —— 比没有按钮更糟。

  现在它是包内的 `ivyea_agent.feishu_relay`，两条命令装好：

  ```bash
  pip install "ivyea-agent[feishu]"   # 飞书官方 SDK，42MB，只有用飞书才需要
  ivyea relay install                 # 写 systemd 单元并启动
  ```

  新增 `ivyea relay run|status|install`。没有 systemd 的机器（含 Windows）
  常驻 `python -m ivyea_agent.feishu_relay` 即可。
  配置向导第 5 步、`ivyea doctor` 的措辞都改成**能直接敲的命令**，
  不再只说一句"未安装"。

  两处打包才会暴露的坑一并修了：会话映射默认从模块目录挪到 `~/.ivyea/relay-state`
  （装进 site-packages 后那里通常不可写）；`import config` 这类裸导入改成包内相对导入
  （否则会去撞用户环境里任何一个同名模块）。旧的手工部署单元名继续认，
  升级不会把一个跑得好好的服务显示成"未安装"。

## [v1.15.10] - 2026-08-23

### 新增

- **飞书卡片重排版**。原来一条异常是一整句话塞进一行，手机上折成三行、重点在句尾；
  上面还压着四行「数据来源：xxx · 延迟约 5 分钟 · 120 行」。现在：
  标题写清构成（「紧急 2 · 注意 3 · 欧洲-UK」而不是「异常 5 条」）；
  一条异常一个块，第一行是**出了什么事**（「断货」「评分偏低」）而不是商品名——
  一屏全是商品名开头时扫不出哪条要紧；关键数字进双列小格（当前 2.9 星 / 门槛 3.5 星 /
  对象 B08N…）；紧急的自动排最前；数据来源、跳过项、规则代码降级成页脚小灰字——
  它们必须留着（"没告警"不能等于"没问题"），但不该占据视线。早报、周报、月报的
  异常列表也换成同一套读法。

- **接上亚马逊官方 API（SP-API + Ads API）**。在 IvyeaOps 系统配置 → 亚马逊官方 API
  里填 LWA 凭据和站点，**填完即用**：巡检规则自动改吃官方数据，
  官方优先、领星兜底（同一个站点两边填同一个 SID 就会自动接上）。
  只用亚马逊、完全不用领星也能跑 —— 店铺清单会退到亚马逊那边登记的站点。

  已接的指标：FBA 库存（SP-API）、广告活动配置与活动日报表（Ads API v3 异步报表）。
  没接的照实说：成本利润（亚马逊不给成本，算不出毛利）和 Listing 快照仍然只有领星有，
  两边是互补不是替代。

  `ivyea amazon verify` 分步自检：凭据 → LWA 换 token → SP-API 真打一次 → 广告档案。
  常见的中间态是「SP-API 批下来了、广告 API 还在排队」，那时前两步该是绿的，
  库存规则照常跑，不该被广告拖着一起停。

  **需要你自己去申请**：亚马逊开发者账号 + SP-API/Ads API 应用审批，周期可能数周。
  代码这边不再是瓶颈，凭据到手当天就能用。

- **异常告警发现即推，且带按钮**。以前定时巡检发现问题只发一段纯文本，
  要动手得等第二天早报里的那张卡；现在 L1/L2 一发现异常就推**带批准按钮**的卡片，
  和早报走同一套审批闭环。

- **告警不再刷屏：只在「正常 → 异常」的那一刻报，恢复时报一条**。
  以前同一条问题每轮巡检重推一次 —— 一个持续一周没处理的断货就是 168 张
  一模一样的卡，人的反应是把群静音，然后真出事的那张也一起看不见了。
  现在持续中的异常静默（crit 每 4 小时、其余每 24 小时最多再提醒一次），
  严重度升级（warn → crit）视作新事件立即报，问题消失时发一条「✅ 已恢复」。
  **取数失败那一轮绝不判恢复** —— 规则没跑不等于问题没了，否则你会在断货最严重
  的那天收到一屏「已恢复」。早报不参与节流：它是汇总，要的就是每天确认一眼。

- **巡检节奏改档，并新增周报与月报**。原来是 L1 每 20 分钟 / L2 每小时 —— 20 分钟和
  1 小时对"库存断货、活动被暂停"这类事没有实质差别，却是三倍的接口调用。
  现在默认 **L1 每小时 · L2 每 12 小时 · 早报每天 · 周报每周 · 月报每月**，
  每一档都能在 IvyeaOps 界面上单独开关和调间隔。
  周报/月报是**回顾**：本期指标环比、本期批了/执行了/回滚了几条、还剩什么没解决；
  **不带审批按钮** —— 同一条建议早报已经给过按钮，两处都给会让同一个目标挂两条待办，
  批了一条另一条还在，容易对同一个活动改两次预算。

- **L3 新增 Listing 维度的销量与广告效率规则**（4 条）：断流、销量下滑、
  listing 级 ACOS 超标、有花费零销售额。活动级报表看不到"哪个货卖不动了"——
  一个活动挂十个 ASIN，其中一个断流，活动整体的 ACOS 可能还很好看。
  这批规则只读指标契约里的多窗口销量字段，换成 SP-API / Ads API 报表后一行不用改。
  整店销量字段全为 0 时报**数据缺口**而不是逐条告警（那 1200 条全是假的）。

- **飞书配置可以在 IvyeaOps 界面里配了**。新增 `/v1/config/feishu`（读状态 / 写凭据）
  与 `/v1/config/feishu/action`（列群 / 列群成员 / 发测试卡片 / 配巡检任务）。
  凭据同时写 `~/.ivyea/.env` 和当前进程的环境变量——只写文件的话，serve 是常驻进程、
  `load_env()` 又明确不覆盖已有环境变量，会出现"界面显示新凭据、发出去的还是旧应用"。

  审批白名单（谁能点卡片按钮）也挪进了 `settings.json`：`feishu_allowed_senders` /
  `feishu_allowed_chats`。relay 现在以它为准、环境变量只作兜底，改完不用重启 relay。
  **留空仍然是"谁都不能点"**，且空列表是有意义的配置（撤权），不会被当成"没配"
  而回退到旧的环境变量名单。

  配巡检任务时会把已存在的 `store_*` 任务一并收编：手工注册过的单店任务和界面上新开的
  全店任务并存，会让同一个店被巡两遍、飞书里出现两张几乎一样的卡。

- **多店铺巡检**。一条计划任务覆盖全部店铺，不必每个店注册一份：
  `ivyea schedule set l1 store_l1 --every-minutes 20 --all-stores`。
  也支持 `--sids 1863,1872` 指定若干店、`--exclude-sids` 排除若干店。
  `ivyea store health --all-stores` 可以当场跑一遍；`ivyea store list`
  列出所有店铺并标注哪些未开通广告。
  本机实测：从只看 1 个店扩到 11 个店（欧洲 9 站 + 阿联酋 + 日本），
  多出来的 10 个店此前完全没人看着。

- **每日早报改成一张汇总卡**。11 个店曾意味着每天早上 11 条推送。
  现在一店一行看状态，异常与「待你决定」跨店合并、按严重度排序——
  需要动手的东西永远在同一个位置。单个店时仍是原来那张明细卡。

- **未开通广告的店铺自动跳过广告规则**。领星店铺列表自带该标志位，
  实测精确命中土耳其站和波兰站。这两个店过去调广告接口稳定报
  「参数不合法」，会被当成取数故障、一小时内触发连续失败告警**且永不恢复**。
  现在它显示为「该店铺未在领星开通广告」，不再计入故障，
  顺带省掉每天 144 次注定失败的调用。


- **店铺业务巡检**。`ivyea store health --sid <SID> [--layer l1|l2|l3]`。
  过去 `ivyea alert` 检的是 agent 自己（队列积压、trace 失败），
  现在有了检**店铺**的：库存断货 / 可供天数不足 / 不可售激增 / 活动被暂停 /
  预算被外部改动 / 花费突增 / 曝光归零 / ACOS 超标 / 销量断崖 / 毛利率下滑…
  共 17 条规则，按数据新鲜度分三层跑（快照 20 分钟 · 日内 1 小时 · 隔日每天）。

- **飞书闭环**。异常推成交互卡片，点「批准执行」直接改领星，卡片原地变绿显示
  审计号并给出回滚按钮。相关命令：`ivyea approval list/show/approve/deny/execute/rollback`。
  接收端是独立的 `feishu-ivyea-relay` 服务（长连接，不需要开放任何公网端口）。

- **每日早报**。昨日广告/店铺/库存指标 + 环比 + 异常 + 待你决定的建议，一张卡片。

- **定时巡检**。`ivyea schedule set <名> store_l1 --every-minutes 20 --sid <SID>`，
  配合 `deploy/systemd/ivyea-schedule.timer`。`schedule set` 新增 `--every-minutes`
  ——分钟级任务写成 `--every-hours 0.333` 既不直观又会漂移。

- **飞书通知通道 `feishu_app`**。走应用身份发交互卡片（原有的 `feishu` 是群机器人
  webhook 纯文本，保留作兜底）。`ivyea notify test --channel feishu_app`。

- **阈值可在飞书里当场调**。`/threshold` 查看，`/threshold <键> <值>` 修改，
  改完立刻生效不用重启。写错键名会直接报错，不会静默用默认值。

### 变更

- **同一个款的多个变体只报一条**。日本站一个母体 ASIN 挂着 30 个变体，
  评分都是 3.0 星，过去会刷出 24 条一模一样的告警，一个款就能把早报占满。
  现在合并成一条并注明「同母体 XXX 下另有 21 个变体同样如此」，信息没少。
  合并只对纯告警生效——带执行按钮的建议**绝不合并**，
  否则点下去说不清改的是哪个目标。合并门槛可调（`variants.collapse.min`，默认 3）。

- 巡检**逐店隔离**：某个店取数失败或抛异常，其余店照常跑完并如实报出坏的那个，
  不再是一个店把整批带崩。连续失败计数仍按店分别记。

- 数据源异常卡片现在会写明是**哪个店**出的问题。


- 巡检发现的问题带**溯源**：每条告警都标明「来源 X · 延迟 Y · N 行」，
  你能判断该多信任它。取不到数时报「数据缺口」而不是静默返回「无异常」
  ——「没告警」不能等于「没问题」。

- 告警有降级链：应用卡片发不出去时退到群机器人 webhook；两条都失败时
  把每条通道的原因都列出来。

### 修复

- **花费突增规则在非小时级采样下会静默失效**。基线取的是"两次采样之间的差值"，
  每小时采一次时它恰好等于小时速率，看着没毛病；采样间隔一改成 12 小时，
  基线变成 12 小时总量、当前值仍是每小时速率，一比差 12 倍 —— 规则永远不触发，
  而且不报错。基线改为速率口径后与采样节奏解耦。同时段匹配也加了 ±3 小时容差：
  任务按"上次跑完 + 间隔"调度，执行时刻会慢慢漂，要求整点严格相等的话，
  漂过一个小时边界基线就凭空消失。

- **Listing 的「近 7 日销量」一直是 0**。领星 `erp_listing` 返回的 116 个字段里
  没有 `seven_volume`（有昨日/14 日/30 日/累计，唯独跳过 7 日），
  而代码在读这个不存在的字段。改为按日均反推。

- **计划任务中途被中断会导致已跑完的任务重跑**。`schedule run-due` 过去把所有任务的
  「上次执行时间」攒到最后一起落盘，中途被杀（systemd 超时、重启）就全丢，
  下一轮整批重跑——对早报就是同一张卡再推一遍。改成跑完一个落盘一个。
  单店时一次 run-due 只要十几秒，这个窗口小到看不见；多店铺巡检把它拉长到几分钟。

- 定时任务的 systemd 超时从 10 分钟放宽到 30 分钟。实测 11 个店铺：
  L1 全店 75 秒、早报 258 秒；重启后的补跑会把 L1/L2/早报凑到同一次执行里，
  再叠上广告报表缓存过期那天的冷启动，10 分钟不够宽。


- 测试隔离：`intraday`/`log`/`schedule`/`workspace`/`self_manage`/`task_runner`/
  `code_agent`/`tools_general` 共 8 个模块在模块级绑定 `config.IVYEA_DIR`
  却不在 conftest 重载列表里，跑测试会跨用例泄漏、甚至写到真实 `~/.ivyea`。

---

## [v1.15.9] - 2026-08-22

### 修复

- **升级现在是完全无感的**。v1.15.8 虽然让"没写过环境字段"的配置保持原样，
  但把**写过 `env`** 也读成了"这个用户要收紧" —— 而 `env` 这个字段在 v1.15.7
  之前是**被静默忽略**的。谁在 `mcp.json` 里写它的时候都不知道有"收紧"这回事，
  他只是想加一个变量，而那个服务器同时还靠 shell / systemd 里的其它变量工作。

  现在 **`inherit_env` 是唯一的收紧信号**：没写就继承全部环境（和 v1.15.6 及
  以前完全一样），`env` / `env_passthrough` 一律在此之上**叠加**。
  所以升级完什么都不用设置，原来能跑的照样跑 —— 而且 `env` 从此真的生效
  （以前写了也没用）。

  安全仍然渐进拿到，走两条主动路径：`ivyea mcp add` 向导给新服务器写
  `inherit_env: false`（新加的默认收紧），以及 `ivyea mcp env <名> --secure`。

  两次弯路的完整记录见
  [ADR-0016 的《修订》](docs/decisions/0016-subprocess-env-allowlist.md)。

---

## [v1.15.8] - 2026-08-22

### 修复

- **v1.15.7 的环境变量收紧会打断现有配置，这版收回**。上一版把默认直接切成白名单，
  结果升级前配好的 MCP server 拿不到它依赖的变量 —— 而报错是那个 server 自己发的
  （"XXX_KEY 未设置"），用户查自己的密钥配置查不出问题，根本猜不到是升级导致的。
  给出的补救还是"去 `mcp.json` 里手加一行" —— 这个产品面向做运营的人，不是开发者。

  现在：**三个环境字段一个都没写的服务器/钩子，保持原样继承全部环境**（即 v1.15.6
  及以前的行为，零打断）。写了任意一个就按你写的走。安全默认改由下面几条渐进拿到。

### 新增

- **`ivyea mcp env <名称>`** —— 不用碰 JSON 就能管一个 stdio 服务器的环境：

  ```bash
  ivyea mcp env 某服务器                    # 看当前策略、哪些变量有值哪些缺
  ivyea mcp env 某服务器 --secure           # 收紧到白名单
  ivyea mcp env 某服务器 --pass GITHUB_TOKEN  # 放行一个
  ivyea mcp env 某服务器 --set KEY=值        # 直接给值
  ivyea mcp env 某服务器 --unset KEY         # 去掉
  ivyea mcp env 某服务器 --inherit          # 放开全部（不推荐）
  ```

- **`ivyea mcp add` 向导会问这个服务器需要哪些环境变量**，并把答案写成显式策略
  —— 所以**新加的服务器默认是收紧的**，且当场提示哪些变量当前环境里没有。
- **`ivyea mcp list`** 每个 stdio 服务器后面直接标出环境策略。
- **`ivyea mcp doctor`** 末尾列出还能读到本机全部环境变量的服务器，并给出收紧命令。

取舍与教训见 [ADR-0016 的《修订》一节](docs/decisions/0016-subprocess-env-allowlist.md)。

---

## [v1.15.7] - 2026-08-22

### 新增

- **明确许可证：MIT**。此前仓库没有 LICENSE 文件、`pyproject.toml` 也没有 license 字段
  —— 那在法律上等于"保留所有权利"，别人不能合法使用、fork 或基于它做二次开发。
  现在 `LICENSE` / 包元数据 / README 三处一致，构建产物里也带上 `License-File`。
- **MCP stdio 服务器和 hook 条目支持三个环境变量字段**：`env`（直接给值，或用
  `${VAR}` 从父进程取一个）、`env_passthrough`（列几个键名原样带过去）、
  `inherit_env`（整份继承，退回旧行为）。

### 变更

- ⚠️ **MCP stdio 子进程和 hook 子进程不再继承本机全部环境变量**。此前
  `Popen` 不带 `env=`、hook 直接 `dict(os.environ)`，意味着任何一个第三方 MCP
  server 或 hook 脚本都能读走 `DEEPSEEK_API_KEY`、领星凭据等等。现在默认只放行
  一份白名单（`PATH` / `HOME` / `LANG` / `TZ` / `TEMP`，Windows 另有 `SYSTEMROOT`
  等必需项），其余要在配置里显式声明。

  **如果你的某个 MCP server 或 hook 脚本依赖环境里的变量**，在它自己的条目里加
  `"env": {"KEY": "${KEY}"}` 或 `"env_passthrough": ["KEY"]`；实在理不清可以先用
  `"inherit_env": true` 退回旧行为。

  取舍与代价见 [ADR-0016](docs/decisions/0016-subprocess-env-allowlist.md)。

### 修复

- **`mcp.json` 里的 `env` 字段此前被静默忽略**。`MCPClient` 根本没读它，配了也不生效
  —— 因为多数 server 到真正调用工具时才校验密钥，`list_tools` 能过，所以一直没暴露。
  本机的 sellersprite 就是这个情况：能列出工具，一调就报"SELLERSPRITE_KEY 未设置"。
  现在 `env` 真的会传给子进程。

---

## [v1.15.6] - 2026-08-21

### 新增

- **Kimi Code 订阅可以授权登录了**。走标准 RFC 8628 设备码，和 Qwen / Codex 同一套流程
  （`POST /v1/auth/kimi-code/start` → 显示代码 → `poll`）。契约取自官方 CLI 包
  `@moonshot-ai/kimi-code` 自己公开的常量：`auth.kimi.com` + `/api/oauth/device_authorization`
  + `/api/oauth/token`。登录后主脑地址走 `https://api.kimi.com/coding/v1`
  （实测该端点同时接 `/messages` 和 `/chat/completions`，这里按 OpenAI 兼容用）。
  token 过期会自动用 refresh_token 续。
- **GLM Coding Plan 加了两条专用 provider**：`zai-coding`（`https://api.z.ai/api/coding/paas/v4`）
  和 `glm-coding`（`https://open.bigmodel.cn/api/coding/paas/v4`）。

### 修复

- **GLM 订阅此前根本接不上**。Coding Plan 官方明确要求用 coding 专用端点
  `/api/coding/paas/v4`，而内置的 `zai` / `glm-legacy` 两条用的是通用端点 `/api/paas/v4`
  —— 拿订阅的 key 填进去不通，报错还完全指不到"地址填错了"上。原来那两条保持不动
  （普通 API key 仍然用它们），订阅走新加的两条。

---

## [v1.15.5] - 2026-08-21

### 新增

- **订阅制 provider 可以从外部调用方登录了**（Claude 订阅 / OpenAI Codex / Gemini
  Code Assist / Qwen / GitHub Copilot）。这几家不是填 API key 而是要走 OAuth，
  此前只有 CLI 的 `ivyea model auth <id> --login` 一条路，不会用命令行的人就被挡在门外。
  新增 `GET /v1/auth` 与 `POST /v1/auth/{id}/start|poll|complete|logout`，
  IvyeaOps 的网页据此把同一套流程做成引导式界面。
  凭据（PKCE verifier / state / device_code / token）**一律不出服务端**，
  返回的只有用户需要看到的东西：授权链接、user_code、验证地址。
- 设备码登录拆出了 `qwen_device_start/poll`、`codex_device_start/poll`，粘码登录拆出了
  `anthropic_login_start/complete`、`google_login_start/complete`。原来的登录函数在
  函数内部轮询到成功为止（最长十几分钟），HTTP 请求挂不住；拆开之后 start 立刻返回，
  中间态由调用方保管。**CLI 的四条登录路径在这些函数之上重新拼起来，行为逐字不变。**

### 变更

- Copilot 的 GitHub Token 现在写进 `COPILOT_GITHUB_TOKEN`，不再往 `GH_TOKEN` /
  `GITHUB_TOKEN` 里塞 —— 那两个是 gh CLI、CI 脚本在用的通用变量，被顺手改掉时
  完全看不出是谁干的。解析顺序上 `COPILOT_GITHUB_TOKEN` 本来就排第一，读取端无感。

---

## [v1.15.4] - 2026-08-21

### 新增

- **一轮对话可以指定用哪个主脑模型**。`chat` / `chat/stream` 认 `model` 字段
  （`<provider_id>:<model>`，如 `openrouter:x-ai/grok-4.6`），只对这一轮生效。
  在此之前主脑是纯全局设置，调用方（IvyeaOps 任务台）想让用户"点一下就换模型"，
  唯一的办法是改全局 —— 那会把别的用户和正在跑的定时任务一起换掉。
  不传 `model` 时行为与此前逐字一致。
  指定的模型没配密钥 / 没有接口地址 / id 不认识时**当场报错**
  （`model_key_missing` / `base_url_required` / `unknown_model`），
  绝不回落到主脑跑完一轮 —— 那样用户选了 A 跑的是 B，还没有任何提示。
- **`POST /v1/model/catalog`**：给 `base_url` + `api_key` 就能列出任意 OpenAI 兼容
  端点的模型清单。原有的 `/v1/model/providers/{id}/models` 只认内置 provider 表里
  那几家、密钥也只从 agent 自己的 .env 取，而中转商（apimart、硅基流动）常常两样
  都不满足。拉不到时（余额不足这类）退回内置清单并带上原因，不让调用方的面板卡死。

### 变更

- `chat` 的返回与 `chat/stream` 的 `start` 事件里，`model` 字段现在报**这一轮真正
  用的模型**而不是全局主脑。不指定 `model` 时两者本来就是同一个，老调用方无感。

---

## [v1.15.3] - 2026-08-21

### 修复

- **附图的内容跟着会话走了**。调用方（IvyeaOps 任务台）读出来的附图文字此前塞在
  `system` 里，而 system 每轮重建、落盘时又被本轮那份整个覆盖 —— 于是"图里是什么"
  只在贴图那一轮存在。用户贴图问完一轮，下一轮问"你是通过什么识别图片的"，模型手里
  一个字都没有，只能否认自己收到过图，还把上一轮如实的描述说成是自己编的（真实投诉）。
  现在 `chat` / `chat/stream` 认 `attachments` 字段（`[{kind,name,ref,by,text}]`），
  内容并进**这一轮的 user 消息**，跟着历史和存档一起走；提示里也讲明图是视觉模型代读的、
  不许否认收到过图。展示端按 `[用户附图` 这个标记截断，气泡里不会看到这段文字。

---

## [v1.15.2] - 2026-08-20

### 修复

- **一轮开跑就落盘，不再是跑完才落盘**。此前整轮只在收尾时写一次盘，于是：跑着的会话
  在工作台左栏根本不存在；中途切走再回来，前端内存里那份没了、磁盘上又没有，整段对话
  凭空消失；断链或中止的轮次一个字都不留。用户看到的是"回到几小时前的某个时间点，
  之后的全部找不到"（真实反馈，连着两三次）。现在用户那句话在模型开口之前就落盘。
- **通用疑问词不再把技能拉进来**。中文查询按 2-gram 切分，「为什」「什么」正好落在技能
  触发词「**为什么**卖不好」里 —— 于是"为什么我的模型配置页面报错"这种和亚马逊毫无关系
  的问题，会以 22 分命中 ASIN 审计手册，然后整轮被那本手册带着跑。现在疑问词/人称/
  礼貌语这类通用片段不参与打分，自动注入还要求名义分达到下限。
- **只读档挡下写操作时说得清楚**。只读档下写操作是直接拒绝的，**不会产生任何待审批项**；
  此前模型只会转述一句"被拦截"，用户跑去待审批页空等（真实反馈："经常跑一半说被拦截，
  待审批那一页也从来没看到任何审批项"）。现在拒绝文案讲明这是档位限制、该切哪一档，
  并在 `final.readonly_blocked` 里回报本轮被挡的次数供界面提示。

### 新增

- **计划变了当场播报**：每次 `todo_write` 落地后，`chat/stream` 多发一个 `todos` 事件
  （`{todos:[{content,status}]}`）。此前计划只随 `final` 回一次，接入方要等这一轮跑完
  才知道"接下来要干什么"—— 而那正是最不需要它的时刻。老接入方不认识这个事件，忽略即可。
- **会话累计账落盘**：会话存档新增 `stats`（`turns` / `steps` / `elapsed_ms` / 累计
  `usage`），会话详情接口一并返回。此前每轮的用时和用量只在流里飘过一次，谁都没记下来，
  于是刷新或换台机器打开同一条会话，"用时 / 输入 / 输出"就全没了。**测不到的项不写**
  —— 补 0 等于替 provider 断言"这轮没花"。
- **`GET /v1/chat/permissions/pending`**：列出此刻真的还阻塞着等人确认的审批 id。
  给工作台的「待审批」页对账用 —— 页面关掉、断链、agent 重启这三种情况下，这一步在
  agent 侧早就收摊了，而调用方那边的记录会永远停在未决。重启后这里自然是空的，
  那正是"全都作废了"的正确答案。

## [v1.15.1] - 2026-08-18

### 新增

- **审批三档**（[ADR-0011](./docs/decisions/0011-approval-tiers.md)）。`chat` / `chat/stream`
  的 `approval` 现在收三个值：`none` 只读（默认，行为与改动前逐字一致）、`remote` 逐项审批
  （每次写入弹确认卡）、**`auto` 完全放行**（本轮写操作不再逐条问人，等价 CLI 的
  `--permission-mode approve-all`）。长任务里一次授权就够，不用守着屏幕点十几次确认。
  认不出的档位值一律按只读处理。
- **上下文用量上报**（[ADR-0012](./docs/decisions/0012-context-usage-snapshot.md)）。
  serve 在第一个 token 之前发一个 `context` 事件、收尾时再随 `final.context` 发一份：
  `{used, window, percent, breakdown{system,tools,messages}, estimated}`。工作台据此画
  上下文进度条 —— 窗口还剩多少、是被系统提示词/工具/对话哪一块吃掉的，一眼可见。
  数是**估算**（按字符，与服务商回报的 prompt_tokens 实测相差约 2%），事件里明说。
  模型窗口按 id 匹配，查不到落 128K 保守值，`ivyea config set context_window` 可覆盖。
  会话详情（`chat/sessions/{id}`）也带一份 `context`，**按整份存档算、不随分页变** ——
  这样调用方打开一条历史会话时立刻就能画出进度条，不必等用户再问一句。

### 修复

- **选了「逐项审批」，模型却仍被告知"当前只读"**。`[IvyeaOps 嵌入模式]` 那句只读提示词
  是无条件拼进系统提示的，于是放开写的两档里模型照样只给方案不动手 —— 界面上开关变了、
  行为一点没变。现在这句话跟着档位走，三档三句话。

---

## [v1.15.0] - 2026-08-18

### 修复

- **一句「测试」不再跑 2 分 16 秒**。v1.14.1 修"复杂度判据只看用户说的话"时，在
  `task_scope.py` 中段又写了一份同名的注入标记清单，把模块顶上那份**整个遮蔽**掉了 ——
  而新的这份恰好漏了技能注入。于是自动命中的技能手册（1600+ 字，满篇"执行/分析/优化"）
  又被当成用户说的话：一句「测试」被判成复杂多步任务，护栏要求先列待办再汇报才准动手，
  线上实测 **18 步里 17 步是 `progress_update`/`todo_write`，工具自身耗时 0.0s**。
  清单合并成一份，并补齐了同样一直在污染判据的**记忆摘要、长期指令/画像、工程上下文**。
  测试改成**按注入点反查**：从源码里抓出所有拼进用户消息的块头逐个对账，漏一个就红。

### 新增

- **按请求性质选执行路线**（[ADR-0010](./docs/decisions/0010-request-routing-lanes.md)）。
  serve 收到一句话先判路线，`start` 事件里带 `lane` 和判定理由：
  - **闲聊**（寒暄 / 问身份 / 极简常识，整句匹配白名单）——不挂工具、不查知识库、
    不注技能，一次调用返回。「1+1等于几？」2.2s，「你好」5.3s，都是 0 步。
  - **板块任务**（市场调研 / 打法 / 关键词竞争 / 审计…）——工具集照样全量，但给一条
    点名到具体工具（如 `market_generate_report`）的直达指令，省掉"先分析一轮再想起来
    该调工具"那几步。
  - **其余**——与改动前逐字一致。判不准一律落这条。

  终端（`ivyea chat` 交互和 `-p`）走同一套判定：闲聊不挂工具、不查知识库、不注技能，
  也不再为一句问候扫一遍工程上下文。真 pty 实测「你好」首字 3.9s，而「列一下有哪些
  一级目录」照常调工具、5.7s 给出结果。
- **技能自动注入只认名义命中**（id / 标题 / 描述 / 触发词对上），不再因为正文里撞了几个
  常用词就把整本手册塞进上下文——「测试」曾以 score=2 命中 ASIN 审计手册。手动
  `skill search` 不受影响，翻库时仍然宽进。

---

## [v1.14.1] - 2026-08-18

### 修复

- **一句话的活不再被判成"复杂多步任务"**。判定复杂度时会连同**系统注入的内容**一起算
  —— 知识检索会往用户那句话后面追加几千字证据（实测 28 字的提问被撑到 3851 字，
  里面自带"执行/分析/诊断"这些词），于是"把某个广告活动的预算改成 8"命中了
  "动作词 + 长度≥60"，整套 todo + 阶段汇报状态机被启动：同一个动作被拦两轮、
  来回几十条消息、用户等了 8 分钟才看到结果。现在判据**只看用户真正打的那部分**，
  检索证据 / 引用门禁 / 人设 / 附图说明这些注入块一律切掉。真正的多步工程照常走汇报闭环。

## [v1.14.0] - 2026-08-17

### 新增

- **技能支持业界通行的 `SKILL.md` + YAML frontmatter 写法**。以前必须在 SKILL.md 旁边再放一个
  `skill.json` 才认，等于给同一件事发明了第二种格式 —— 外部技能库（比如 IvyeaOps 的 Skill 中心，
  近百个技能）一个都加载不进来。现在两种都认：`skill.json` 在就按它（老技能一个不动），
  不在就读 frontmatter 的 `name` / `description` / `description_zh` / `version` / `triggers`
  （也兼容 `metadata.hermes.tags`）。
- **可以挂外部技能库**：`IVYEA_SKILL_ROOTS=/a/skills:/b/skills`（Windows 用 `;`），或
  settings.json 里的 `skill_roots`。技能**原地加载，不复制**，那边改完这边立即生效。
  外部库**不能覆盖内置技能**（同 id 时内置优先）—— 顶掉内置技能是很难查的故障。
  个人技能（`~/.ivyea/skills`）保持原有的覆盖语义。
- **带附属文件的技能会告诉模型文件在哪**。技能正文常写着「运行 `scripts/xxx.py`」「参见
  `references/xxx.md`」，以前模型拿不到路径，只能瞎找或凭正文硬编；现在 `render_skill` 和
  自动匹配的上下文里都带上绝对目录。没有附属文件的技能不加这句。

### 修复

- **中文提问现在能匹配到技能了**。分词那一步把「做个市场调研看看这个类目」整串当作**一个**词，
  而它不可能出现在任何技能文本里 —— 中文查询的匹配分因此恒为 0。现在中文按 2-gram 切
  （「市场调研」→ 市场 / 场调 / 调研），中英混排的「asin审计」也能切到。
- **技能排序不再被长正文带偏**。切了 2-gram 之后，正文长的技能靠噪音词堆分，实测一个几千字的
  审计技能能在完全不相干的查询上排第一。现在标识/标题/描述加权、正文命中封顶。
- **写成「调研报告、选品调研、市场分析」的触发词会被拆开**。整串当作一条触发词既匹配不准，
  又会把不相干的查询拉进来。

### 变更

- 新增依赖 `PyYAML`（解析 frontmatter）。加载器对它缺失是容错的 —— 老环境升级上来只会跳过
  frontmatter 格式的技能，不影响 `skill.json` 那批。

## [v1.13.1] - 2026-08-16

### 修复

- **`ivyea service stop` 会谎报成功**。它只认 pidfile，进程实际还活着也会报「已停止」。
  这条 bug 的真实杀伤力在于：升级流程以为停干净了，于是新版装上去、旧进程还在跑，表现为
  **「明明升级了却还在跑旧代码」**，而且完全没有报错。
- **`ivyea self update` 指向了一个不存在的 PyPI 包**。源码安装的走 `git pull`，其余情况
  才回落到 pip/pipx。

## [v1.13.0] - 2026-08-16

### 新增

- **视觉三档降级链**。主脑没有视觉能力不再等于「带图就废」：
  1. 主脑本身支持视觉 → 直接读图
  2. 主脑不支持、但配了独立视觉模型 → 旁路代读，结果回灌给主脑
  3. 两者都没有 → 本地 CV + OCR 做量化描述

  关键是**降级了会明说**，不会让模型对着一张它根本没看见的图编造画面。

## [v1.12.0] - 2026-08-16

### 新增

- **自带语义检索，装完就有**。随包一个 int8 量化的 ONNX 模型，不需要额外配 embedding API，
  也不需要本地部署模型。相比原来的纯词法检索，MRR 提升 27%。

  这条同时解决了一个开源分发问题：以前用户下载后语义检索默认是关的，手动开又很费劲。

## [v1.11.0] - 2026-08-15

### 变更

- **记忆系统重做**。三层架构（核心 / 分类 / 情景）+ 语义与词法双路召回（RRF 融合）+
  评测框架。配套做了时效双时间轴、溯源置信度、遗忘机制、关联图、待定区和并发锁。

  重做的起因是一个方向性错误：**原来的「语义」其实是对词法候选做重排**，词法零命中时语义
  根本没有机会参与。真正的双路召回是两路独立检索再融合。

## [v1.10.0] – [v1.10.3] - 2026-08-13 → 08-14

### 新增

- **思考流推给网页端**，纯 LLM 耗时单独统计；历史详情按轮分页；执行过程落盘。

### 修复

- **网页端把同一段正文吐好几遍** —— 正文分段边界的问题。
- 打 tag 时校验版本号与 tag 一致，防止两者漂移。

## [v1.9.0] - 2026-08-08

### 新增

- **结构化步骤事件与远程审批**。工作台可以把 agent 的每一步渲染成独立分区，审批也能在网页
  （包括手机）上点，不用回到终端。
- **文件变更事件** —— 工作台「产物栏」里 diff 那一格的数据源。

### 修复

- 会话安全与并发问题。

## [v1.8.0] – [v1.8.8] - 2026-07-09 → 07-29

### 新增

- **Amazon 知识库扩展到 CA / MX 站**，税务部分本地化（VAT / JCT）。
- **内置 ASIN COSMO/Rufus 审计 skill**，配合显式 skill 注入和可信数据源向导 —— 让 agent
  真的能跑完整的 ASIN 深度审计。
- `/v1/chat*` 支持图片输入，codex 通道标记视觉能力。
- `/v1/chat/sessions/import` —— 迁移时可以把已有会话灌进来。

### 修复

- **「经常撞工具步数上限」**：工具调用预算调高，让一轮能真正跑完。
- **`/v1/chat/stream` 加 15s 心跳，客户端断开不再打断轮次** —— 长任务不会因为前端超时就白跑。
- 带知识引证的回答恢复流式输出（`defer_citation_text` 开关）。
- **单一版本源**，根治 `__version__` 漂移导致的「永远提示有更新」。

## [v1.7.0] – [v1.7.1] - 2026-07-06

### 新增

- **受治理的 Amazon 知识平台**。知识以卡片形式管理，带来源、覆盖度和冲突检测。

### 修复

- 知识冲突判定不再命中过于宽泛的匹配。

## [v1.3.0] – [v1.6.0] - 2026-07-05

### 新增

- **思考深度旋钮 + 完成前自验证门禁 + 通用自我批判 + 多轮修复**。agent 在说「做完了」之前
  必须自己验一遍。
- **Claude(Anthropic) 订阅版 OAuth 登录** —— 不用 API Key 也能用。
- **Claude extended thinking** 接入（工具循环里保留 thinking 块）。
- 结构化进度上报；任务范围界定与终端输出改进。

## [v1.2.0] – [v1.2.4] - 2026-07-03 → 07-05

对标 headless / 无人值守场景的六项升级：

### 新增

- **`chat -p --output-format stream-json`** —— 结构化 NDJSON 输出。这是 IvyeaOps 能把 agent
  当 runner 驱动的基础。
- **hooks**：`pre_tool_use` / `post_tool_use` / `stop` / `session_end` 事件。
- **压缩保留最近 N 条消息原文**，不再把近期上下文一并摘要掉。
- **视觉旁路**：对话带图时，主脑没有视觉能力就让 sidecar 代读回灌。
- **子 agent 并行 fan-out**，只读工具子集。
- 命令输出落盘、`--permission-mode policy` 档、token 估算校准。

### 修复

- **codex 主脑的用量和花费一直显示 0** —— Responses API 的 usage 结构没有归一成内部契约。
- 冻结的 GUI exe 里跑 `ivyea` 崩 `AttributeError`（isatty 返回 None）。

## [v1.1.0] – [v1.1.7] - 2026-07-02 → 07-03

对标 Claude Code 补齐六项能力：

### 新增

- **`@文件引用`**（内联文件内容 + 路径补全）
- **对话图片输入**（`@图片路径` + `/paste` 剪贴板，四个 provider 全打通）
- **思考可见** —— reasoning 流渲染成 dim 的思考行
- **渐进式 markdown 流式**，消除整段「原文 → 渲染」的跳变
- **`/rewind` 检查点** —— 对话和代码快照一起回退
- **后台 / 长任务 bash**：`run_in_background` + `bash_output` + `kill_bash`
- **`chat -p/--print` 非交互一次性模式**（给 IvyeaOps 这类调用方做 runner 用）
- **`self update` 子命令**：源码装的走 git pull，否则 pip / pipx
- 启动注入 `MEMORY.md` 摘要，开箱就知道 agent 记得什么

### 变更

- 输入框改为**钉底常驻**（滚动区常驻 app 模式），生成过程中位置固定，原生滚轮和复制都保留。

## [v1.0.29] – [v1.0.45] - 2026-06-30 → 07-02

### 新增

- **全屏 TUI 聊天界面**并设为默认（`IVYEA_TUI=0` 可退回行式）。分 P0–P5 六步做完：
  骨架 → 核心闭环 → 中断与排队 → 审批内联面板 → 输入对齐 → todo 计划面板。
- 写代码体验对标 Claude Code / Codex：行号、glob、auto-edit、只读命令放行、改前必读硬护栏、
  `/diff`、流式正文预览、上下文进度条、自动压缩默认开。

### 修复

- TUI 一连串交互问题：transcript 可滚动、贴底跟随与滚动兼得、「生成中」移到内容处、
  指令不再回显两遍、输入框不再撑满屏幕。

## [v1.0.18] – [v1.0.28] - 2026-06-24 → 06-25

### 新增

- **与 IvyeaOps 打通**：agent 可以调用工作台各板块的工具生成报告（ops-bridge）。
- **MCP 双向对等** —— chat 工具、resources、prompts 两侧都支持。
- **`dispatch_subagent`** 只读扇出探查工具。
- **自定义斜杠命令 + 用户 hooks**。
- **沙箱**：`run_command` / `run_python` 加 POSIX rlimits。
- 轮次中自动压缩，防止上下文溢出；独立只读工具调用并行执行。
- 会话可删除（`DELETE /v1/chat/sessions/{id}`）。

### 修复

- 后台 serve 用 `pythonw.exe` 启动，Windows 上不再弹出控制台窗口。

## [v0.5.0] – [v0.5.13] - 2026-06-19 → 06-23

### 新增

- **嵌入式服务与本地检索**：对外暴露只读 chat API、只读 MCP server、任务状态 API，
  IvyeaOps 由此可以把 agent 当成一个常驻服务来用。
- 本地稀疏向量检索 + 持久化检索索引；可选的语义检索后端。
- 通用 agent 能力：git 写操作、补丁工作流、GitHub CI 状态查询。
- 在线 provider 模型目录；从模型选择器直接发起 OAuth 登录。
- 离线部署包；门户站 agent.ivyea.com。

### 修复

- Windows 上 Qwen CLI 的鉴权路径。

## [v0.4.0] - 2026-06-18

首个发布版本。**影子模式** —— 动真钱之前先用数据换信任：agent 把它「本来会做的动作」
记录下来但不执行，跑一段时间后拿实际数据对照，确认判断可靠了再放开写权限。

在此之前（2026-06-16 → 06-17，尚未发版）已完成的骨架：

- **P1** 领星只读广告巡检 MVP；**P1.5** 通用 MCP 客户端 + 可配置字段映射
- **P2** 审核制执行（否词 / 调价）+ 硬护栏 + 审计回滚
- **P2.5** 对话式 agent（`ivyea chat`）+ Claude Code 式权限审批 + 工具调用循环
- **P3** 记忆系统（SQLite FTS5 + 策展 markdown + 自策展）
- **M1–M6** 对标 Claude Code / Codex / Hermes 的十个里程碑：对话内核重做（流式 + 成本核算
  + 计划模式）、上下文压缩、会话 resume、Claude 原生 provider、通用工具层 + 沙箱 + 工具级
  审批门控、Todo/Plan 面板、彩色 diff、eval 回归集 + CI
- **Tier 1** 限流重试与降级链（主脑挂了自动切备用）；**Tier 2** 领星缓存 + 进度 + 可中断
  + 成本护栏

---

[v1.13.1]: https://github.com/Hector-xue/ivyea-agent/releases/tag/v1.13.1
[v1.13.0]: https://github.com/Hector-xue/ivyea-agent/releases/tag/v1.13.0
[v1.12.0]: https://github.com/Hector-xue/ivyea-agent/releases/tag/v1.12.0
[v1.11.0]: https://github.com/Hector-xue/ivyea-agent/releases/tag/v1.11.0
[v1.9.0]: https://github.com/Hector-xue/ivyea-agent/releases/tag/v1.9.0
[v0.4.0]: https://github.com/Hector-xue/ivyea-agent/releases/tag/v0.4.0
